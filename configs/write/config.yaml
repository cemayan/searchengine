version: 1
environment: dev
serve:
  port: 8088
cache:
  enabled: true
scraper:
  server:
    host: "localhost"
    port: 50077
db:
  selectedDb:
    read: mongodb
    write: mongodb
  cache:
    name: "redis"
    addr: "localhost:6379"
  persistent:
    name: "mongodb"
    uri: "mongodb://localhost:27017"
