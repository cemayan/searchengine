version: 1
environment: dev
serve:
  port: 8087
db:
  selectedDb:
    read: mongodb
  cache:
    name: "redis"
    addr: "localhost:6379"
  persistent:
    name: "mongodb"
    uri: "mongodb://localhost:27017"